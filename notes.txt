create github repo pages for each project
add links to actual repo pages
make accordion arrows white when open
    the arrows are svgs, so that may not be as easy as I thought
    look into using fontawesome icons instead of the default arrows